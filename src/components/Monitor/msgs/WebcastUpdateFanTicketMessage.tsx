import { Message } from "../types";

export type Props = {
    msg: Message;
};

export const WebcastUpdateFanTicketMessage = ({ msg }: Props) => {
    return null;
};

export default WebcastUpdateFanTicketMessage;